# Balanced profile: 13-frame chunks => 4 latents/chunk, 8 net new frames
# (320 ms @ 25fps) per generation. Fallback if the GPU cannot sustain the
# 160 ms budget of the lowlat profile at pro quality — bench/bench_latency.py
# reports which profile your hardware sustains.
frame_num: 13
motion_frames_latent_num: 2
tgt_fps: 25
sample_rate: 16000
sample_shift: 5
color_correction_strength: 1.0
cached_audio_duration: 8
num_heads: 12
height: 512
width: 512
